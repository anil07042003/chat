PORT=3001
JWT_KEY=baatchit
DATABASE_URL=mongodb+srv://iedge388_db_user:qwer1234@cluster.mongodb.net/baatchit
PEPPER_STRING=dev_pepper
NODE_ENV=production

# Allowed origins for CORS (production only — dev allows all origins)
# Comma-separated list of frontend URLs:
ORIGIN=http://localhost:3000,http://localhost:5173
