PORT=3001
JWT_KEY=your_super_secret_jwt_key_change_in_production
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/baatchit
PEPPER_STRING=your_pepper_string_change_in_production
NODE_ENV=production

# Allowed origins for CORS (production only — dev allows all origins)
# Comma-separated list of frontend URLs:
ORIGIN=http://localhost:3000,http://localhost:5173
